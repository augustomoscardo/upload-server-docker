apiVersion: v1
kind: Pod

metadata:
  name: widget-server
  namespace: widget

spec:
  containers: 
    - name: widget-server
      image: augustomoscardo/widget-server:cdd64bd
      ports: 
        - containerPort: 3333